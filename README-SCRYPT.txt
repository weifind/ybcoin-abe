Scrypt python module setup
=======================================

On debian-like system you can try something like this:

apt-get install python-all-dev
cd litecoin_scrypt
python setup.py install

